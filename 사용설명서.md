# RF 기술이슈 관리 시스템

고객사에서 발생한 **RF Generator / RF Matcher(Matching Box) 및 기타 장비**의 기술 이슈를
등록·검색하고, 사내 공유폴더의 기술자료와 연결해 **현장에서 몇 초 안에 대응 방법을 찾는** 도구입니다.

---

## 1. 실행 방법

`RF이슈관리 시작.bat` 을 더블클릭하면 브라우저가 열립니다. (주소: http://localhost:7331)

- 창(검은 콘솔)을 닫으면 프로그램이 종료됩니다. 사용 중에는 열어 두세요.
- Node.js 가 필요합니다. 이미 이 PC에는 설치되어 있습니다.
- 프로그램은 **내 PC에서만** 동작합니다(외부 접속 불가).

---

## 2. 화면 구성

### 통합검색 (기본 화면)
검색어 하나로 아래 3가지를 동시에 찾습니다.

| 종류 | 내용 |
|---|---|
| **이슈** | 내가 등록한 실제 대응 사례 (가장 위에 표시) |
| **지식·용어** | 업무정리 엑셀의 항목 이름과 셀 메모(점검 항목) |
| **자료 파일** | 공유폴더 안의 모든 문서·도면·매뉴얼 파일 이름 |

- 결과의 **[열기]** 를 누르면 공유폴더의 원본 파일이 바로 실행됩니다.
- **[위치]** 는 그 파일이 있는 탐색기 폴더를 엽니다.
- 검색창 아래 자주 쓰는 단어 버튼(전반사, VSWR, 냉각수, D-Net …)을 눌러도 됩니다.

#### 🎤 음성으로 검색하기
검색창 오른쪽 **🎤** 버튼을 누르고 말하면 그대로 검색어가 됩니다.

- 처음 한 번은 브라우저가 마이크 권한을 묻습니다. **허용**을 누르세요.
  나중에 바꾸려면 주소창 왼쪽 자물쇠 → 마이크 → 허용.
- 버튼이 빨갛게 깜빡이는 동안 듣습니다. 말을 멈추면 저절로 끝나고,
  다시 누르면 중간에 멈출 수 있습니다.
- **Chrome 과 Edge 에서만 됩니다.** 그 밖의 브라우저에서는 🎤 버튼이 아예 보이지 않습니다.

말할 때 요령:

| 잘 되는 말 | 잘 안 되는 말 |
|---|---|
| 증상 그대로 — "냉각수 부족 알람", "반사파가 튄다" | 긴 문장 — "지난주에 파주에서 있었던 그거" |
| 짧은 용어 — "전반사", "누수", "매칭박스" | 영문 모델명 — "RFK300FH" (숫자·알파벳은 잘 못 알아듣습니다) |

> 모델명·알람코드처럼 알파벳과 숫자가 섞인 말은 음성 인식이 약합니다.
> 그런 건 **직접 입력**하는 편이 빠릅니다.

> ⚠️ 음성 인식은 브라우저(Chrome·Edge) 기능이라 **말한 내용이 구글 서버를 거쳐**
> 글자로 바뀝니다. 고객사명이나 이슈 내용을 말하는 것이 사내 보안 정책에 걸린다면
> 🎤 버튼을 쓰지 말고 직접 입력하세요. (검색 자체는 전부 사내 PC 안에서만 돕니다.)

#### 연관어 — 다르게 말해도 찾아 줍니다
같은 뜻인데 사람마다 다르게 부르는 말을 묶어 두었습니다.
**"쿨링워터"** 라고 하면 **"냉각수"** 자료가, **"브이에스더블유알"** 이라고 하면
**"VSWR"** 자료가 나옵니다. 신입이 정확한 용어를 몰라도, 음성 인식이 한글로
받아 적어도 찾아집니다.

연관어로 넓혀 찾은 경우 검색창 아래에 이렇게 알려 줍니다:

> 🔎 **쿨링워터** → 냉각수, cooling water, 칠러 도 함께 찾았습니다

**말 묶음 고치는 법** — `data\synonyms.json` 을 메모장으로 열면 됩니다.
한 줄이 한 묶음이고, 그 안의 말은 서로 같은 뜻으로 취급됩니다.

```json
["냉각수", "쿨링워터", "쿨링 워터", "cooling water", "칠러"],
["앵커핀", "Anchor pin", "앙카핀", "앙카 핀"],
```

- 현장에서 자주 쓰는 은어·줄임말을 발견하면 그 줄에 계속 덧붙이세요.
- 고친 뒤에는 **프로그램을 껐다 켜야** 반영됩니다.
- 쉼표와 대괄호를 빠뜨리면 사전 전체가 무시됩니다(검색은 그대로 되지만
  연관어만 안 먹습니다). 시작 화면의 `연관어 : ○○묶음` 숫자로 확인하세요.
- **주의:** 이 파일은 고객사명이 들어 있어 `data\` 폴더에 있고, 자동 백업이나
  git 에 올라가지 않습니다. kb.json 과 달리 **다시 만들어 주지 않으니**
  공유폴더에 따로 복사해 두세요.

#### 신입 교육 가이드 (검색어가 비어 있을 때)
검색창을 비워 두면 **📘 신입 교육 가이드**가 나옵니다. 세 화면을 순서대로 눌러 들어갑니다.

```
① 유형 선택  →  ② 점검 단계(흐름도)  →  ③ 항목 · 자료 열기  →  ④ 이슈 등록
```

| 유형 | 내용 |
|---|---|
| **RFG** RF Generator | 파워를 만드는 본체. 전원 → 냉각수 → 구조 → 센서 → 파라미터 |
| **M/B** Matching Box | 정합기. VVC/VFC, Vpp·Vdc·VSWR 센서 |
| **TC** Total Controller · 제어/통신 | RF controller · MRCS unit · Phase box, D-Net / CAN |
| **공통** 납품 업무 | 업체별 정보, 납품 서류, 출입 절차 |

- 유형을 누르면 **현장 점검 순서대로 흐름도**가 세로로 펼쳐집니다. 동그라미 숫자는 업무정리 엑셀의 항목 번호와 같습니다.
- 단계를 누르면 그 단계의 항목·엑셀 셀 메모·자료가 나옵니다. **[자료 열기]** 로 원본을, **[기술노트 보기]** 로 엑셀 상세 시트 내용을 봅니다.
- **[← 이전 / 다음 단계 →]** 로 흐름을 따라가며 볼 수 있습니다.
- 맨 아래 *"조치 완료 → 이슈 관리"* 를 누르면 이슈 등록 화면으로 넘어갑니다.

> 새 자료가 엑셀에 추가되면 **설정 → 재색인** 후 가이드에도 자동 반영됩니다.

### 이슈 관리
등록된 이슈 목록입니다. 상태·고객사로 거르고, CSV로 내보낼 수 있습니다.

### 지식맵
업무정리 엑셀 **Main page** 를 그대로 옮긴 화면입니다.
- 왼쪽 색 막대: <span>파란</span> RF Generator / <span>주황</span> Matching Box / <span>보라</span> 공통·비고
- `✎` 표시가 있는 항목은 **엑셀 셀 메모(점검 항목)** 가 들어 있습니다. 마우스를 올리면 보입니다.
- `✕` 표시는 링크된 파일을 현재 찾을 수 없다는 뜻입니다.

### 설정
- **엑셀에서 다시 읽어오기(재색인)** : 공유폴더의 엑셀이나 자료가 바뀌었을 때 누릅니다.
  원본 엑셀은 서버가 1분마다 스스로 확인해 다시 색인하므로, 보통은 누르지 않아도 됩니다.
- **자동 갱신** : 마지막으로 확인한 시각과 자동 재색인한 시각을 보여 줍니다.
- **추가 엑셀** : 내용까지 검색되는 엑셀 폴더 목록입니다. (6장 참고)
- **깨진 링크 점검** : 엑셀 하이퍼링크 중 파일이 사라진 항목 목록입니다. 정리 대상입니다.

---

## 3. 이슈 등록하는 법 (가장 중요)

`이슈 관리 → ＋ 새 이슈 등록`

미래의 나(또는 동료)가 **그대로 따라 하면 해결되도록** 적는 것이 핵심입니다.

| 칸 | 이렇게 적으세요 |
|---|---|
| **제목** | 고객사 + 장비 + 한 줄 증상. 예) `○○전자 △△사업장 30kW RFG 전반사 알람 반복` |
| **현상** | 고객이 호소한 내용, 알람 화면, 실제 측정값 |
| **원인** | 점검해서 확인된 진짜 원인 |
| **조치 방법** ★ | `1) … 2) … 3) …` 순서대로. **여기가 나중에 가장 많이 검색됩니다** |
| **결과** | 조치 후 상태, 재발 여부 |
| **재발 방지** | 같은 문제를 막기 위한 조치, 주의사항 |
| **알람 코드** | 검색이 잘 되도록 꼭 넣으세요 (예: `WaterShortage`, `Over REF`) |
| **태그** | 검색용 키워드를 쉼표로. 예) `전반사, Anchor pin, 재발` |
| **관련 자료** | 아래 참조 |

### 관련 자료 연결
- 입력칸에 단어를 치면 **공유폴더 자료와 엑셀 항목이 자동으로 뜹니다.** 눌러서 연결하세요.
- `내 PC 파일 첨부` 는 사진·보고서 등 내 PC의 파일 경로를 연결합니다.
  (파일을 복사하지 않고 **경로만** 저장하므로, 공유폴더에 올려 둔 파일을 연결하는 것이 안전합니다.)

---

## 4. 데이터가 저장되는 곳

```
rf-issue-db\
 ├ data\issues.json          ← 등록한 이슈 (진짜 자산. 백업 대상)
 ├ data\issues.backup.json   ← 저장할 때마다 자동 백업본
 ├ data\kb.json              ← 엑셀·공유폴더에서 만든 색인 (언제든 재생성 가능)
 └ config.json               ← 공유폴더 경로 설정
```

- **`data\issues.json` 만 백업하면 됩니다.** 나머지는 재색인으로 다시 만들어집니다.
- 팀원과 공유하려면 `rf-issue-db` 폴더 전체를 공유폴더에 두고 각자 실행하면 됩니다.
  (동시에 여러 명이 **등록**하면 마지막 저장이 이깁니다. 검색·조회는 동시에 해도 안전합니다.)

---

## 5. 설정 바꾸기 — `config.json`

> 저장소에는 `config.json` 이 없습니다(사내 경로 포함). 처음 받으셨다면
> `config.example.json` 을 `config.json` 으로 복사한 뒤 경로를 채워 주세요.

```json
{
  "sourceExcel": "원본 업무정리 엑셀 전체 경로",
  "baseDir":     "고객서비스 폴더 경로 (하이퍼링크 기준점)",
  "scanDirs":    ["업부정리 엑셀에 인용한 자료", "자료방"],
  "extraExcels": ["\\<NAS-IP>\<공유폴더>\<추가 엑셀 폴더>"],
  "allowedRoots": ["\\<NAS-IP>\<공유폴더>\<추가 엑셀 폴더>"],
  "autoRefreshSec": 60,
  "port": 7331,
  "host": "0.0.0.0"
}
```

- **엑셀 파일 이름이 바뀌면** `sourceExcel` 을 새 이름으로 고치고 재색인하세요.
- `scanDirs` 에 폴더를 추가하면 그 폴더의 파일도 검색 대상이 됩니다.
- `allowedRoots` : 공유폴더 밖(예: `D:\현장사진`)의 파일을 열려면 그 폴더를 여기에 추가하세요.
  보안상 등록되지 않은 경로는 열리지 않습니다.
- `extraExcels` : **엑셀 안의 내용까지** 통합검색에 넣고 싶은 폴더(또는 파일 하나)입니다.
  아래 "6. 추가 엑셀 자료" 를 보세요. 여기에 넣은 폴더는 파일 이름 검색에도 함께 잡힙니다.
- `autoRefreshSec` : 원본 엑셀이 바뀌었는지 확인하는 주기(초). 기본 60초이고 `0` 이면 끕니다.

---

## 6. 추가 엑셀 자료 — 폴더에 넣어 두면 알아서 검색됩니다

`config.json` 의 `extraExcels` 에 적어 둔 폴더의 `.xlsx` 는 **표의 한 줄이 검색 항목 하나**가 됩니다.

현재 등록된 폴더:

```
\<NAS-IP>\<공유폴더>\<추가 엑셀 폴더>
```

**어떻게 검색되나**

- 표의 머리글(첫 줄)을 알아서 찾아 `고객사: ○○○ | 발주서번호: P2026XXXXXXX | …` 처럼 이름표를 붙여 둡니다.
  그래서 `P2026XXXXXXX` 같은 발주서번호, `WTXXXX` 같은 L/N, `○○○` 같은 고객사명으로 바로 찾을 수 있습니다.
- 검색 결과의 **열기** 를 누르면 그 엑셀 파일이 열립니다.
- 한 시트에 표가 두 개 이상 있어도(예: 아래쪽 `무상건` 표) 각 표의 머리글을 따로 인식합니다.

**새 파일을 올릴 때**

폴더에 새 `.xlsx` 를 넣거나 기존 파일을 고쳐 저장하면 됩니다.
서버가 **1분 안에 알아채고 스스로 다시 색인**하므로 따로 누를 것이 없습니다.
파일 이름에 날짜가 붙어도(`…(26.08.25).xlsx`) 그대로 두면 됩니다 — 폴더 안의 모든 엑셀을 읽습니다.

> 급할 때는 `설정 → 엑셀에서 다시 읽어오기` 를 눌러 바로 반영할 수 있습니다.
> 자동 갱신이 언제 돌았는지는 `설정 → 자동 갱신` 줄에서 확인합니다.

**주의**

- 엑셀을 **열어 둔 채로 두면** 저장 전까지는 반영되지 않습니다. 저장 후 닫아 주세요.
- 다른 폴더를 추가하려면 `extraExcels` 와 `allowedRoots` 양쪽에 그 경로를 넣어야 합니다.
  (`allowedRoots` 에 없으면 검색은 되지만 "허용되지 않은 경로입니다" 로 열리지 않습니다.)

---

## 7. 자주 있는 문제

| 증상 | 해결 |
|---|---|
| 검색 결과가 0건 | 설정 → 재색인. 공유폴더(`\\<NAS-IP>`)에 연결돼 있는지 확인 |
| [열기]를 눌렀는데 "파일이 존재하지 않습니다" | 원본이 이동·삭제됨. 설정 → 깨진 링크 점검에서 확인 |
| "허용되지 않은 경로입니다" | `config.json` 의 `allowedRoots` 에 그 폴더를 추가 |
| 새로 올린 엑셀이 검색에 안 나옴 | 엑셀을 저장하고 닫았는지 확인. 1분 뒤에도 안 나오면 설정 → 재색인 |
| 포트 충돌 | `config.json` 의 `port` 를 7332 등으로 변경 |
| 재색인이 아주 느림 | 공유폴더 응답 지연. VPN/네트워크 상태 확인 후 재시도 |
